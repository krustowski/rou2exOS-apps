# eth

A userland Ethernet networking driver. 

| Argmuent | Pair value (if any) | Meaning | Required |
|----------|---------------------|---------|----------|
| `--ip`   | IPv4 address        | A static IPv4 address to assign the NIC. | No |
| `debug`  | *none*              | Starts the verbose mode. | No |

